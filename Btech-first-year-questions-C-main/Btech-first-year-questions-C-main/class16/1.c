//Q1.Wap input a string and display it.
#include<stdio.h>

int main()
{
    char str[100];
    printf("Enter the string: ");
    gets(str);
    printf("The string is: ");
    puts(str);
    return 0;
}