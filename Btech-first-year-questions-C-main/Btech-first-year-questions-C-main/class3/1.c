#include<stdio.h>

int main()
{
    //Q1.WAP to perform the addition of two integers and display the result.
    int a,b;
    printf("Enter 2 numbers: ");
    scanf("%d %d",&a,&b);
    printf("%d + %d = %d",a,b,a+b);
    return 0;
}