/*
Print 1(true) or 0(false) for the following statements:

 a) If it's sunday & it's snowing -> TRUE
*/

#include<stdio.h>
int main()
{
    int isSlowing =1;
    int isSnowing =1;
    printf("%d \n", isSlowing && isSnowing);
    return 0;

}