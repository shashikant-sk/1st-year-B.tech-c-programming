// Print 1(true) or 0(false) for the following statements:

// b.) If its monday 0R || its raining -> True 

#include<stdio.h>
int main(){
    int isRaining =1;
    int isMonday =0;
    printf("%d \n", isMonday || isRaining);
    return 0;
}